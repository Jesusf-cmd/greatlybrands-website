# Copy this file to `.env.local` and fill in verified values only.
# Do not invent a public business email address.

# Optional. Used only if/when CRM delivery is approved.
# GHL_WEBHOOK_URL=

# Optional future notification address. Leave unset until a real mailbox exists.
# BUSINESS_EMAIL=
